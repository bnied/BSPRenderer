#pragma once

#include <vector>
#include "types.hpp"

// Hand-authored map. See level.cpp for the ASCII sketch and definitions.

extern const std::vector<Vec2>    vertices;
extern const std::vector<Sector>  sectors;
extern const std::vector<LineDef> linedefs;
